<%= form_with(model: moto_blog, local: true) do |form| %>
  <% if moto_blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(moto_blog.errors.count, "error") %> prohibited this moto blog from being saved:</h2>

      <ul>
      <% moto_blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="card">
    <div class="card-encabezado-ms">
      <span class="card-title yellow-text">MOTO BLOG</span>
      <%= form.submit "Save Opinion", class: "yellow-text waves-effect grey btn right" %>
    </div>
    <div class="card-content card-fondo">
      <div class="row">
        <div class="col s12">
          <div class="input-field strong">
            <%= form.label :title %>
            <%= form.text_field :title %>
          </div>
          <div class="input-field materialize-textarea strong">
            <%= form.label :story %>
            <%= form.text_field :story %>
          </div>
          <div class="input-field strong">
            <%= form.label :author %>
            <%= form.text_field :author %>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>
